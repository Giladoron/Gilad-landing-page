# EmailJS Configuration
# Required: Get your public key from EmailJS dashboard
# https://dashboard.emailjs.com/admin/integration
VITE_EMAILJS_PUBLIC_KEY=your_emailjs_public_key_here

# Optional: EmailJS Service ID (has default: 'service_fphe5xu')
# Only override if using a different EmailJS service
VITE_EMAILJS_SERVICE_ID=service_fphe5xu

# Optional: EmailJS Template ID (has default: 'template_8p1hgtg')
# Only override if using a different EmailJS template
VITE_EMAILJS_TEMPLATE_ID=template_8p1hgtg

# Optional: Recipient Email (has default: 'gilad042@gmail.com')
# Only override if sending to a different email address
VITE_RECIPIENT_EMAIL=gilad042@gmail.com

# Instructions:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Replace 'your_emailjs_public_key_here' with your actual EmailJS public key
# 3. Optional: Override other variables if needed
# 4. .env.local is gitignored and will not be committed

